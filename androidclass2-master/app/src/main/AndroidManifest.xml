<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.fjnu.cse.helloworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LineActivity">
            <intent-filter>
                <action android:name="android.intent.action.Line" />

                <category android:name="android.intent.category.Line" />
            </intent-filter>
        </activity>
        <activity android:name=".RelativeActivity">
            <intent-filter>
                <action android:name="android.intent.action.Relative" />

                <category android:name="android.intent.category.Relative" />
            </intent-filter>
        </activity>
        <activity android:name=".TableActivity">
            <intent-filter>
                <action android:name="android.intent.action.Table" />

                <category android:name="android.intent.category.Table" />
            </intent-filter>
        </activity>
    </application>

</manifest>